/**
 * Visual height of the main bottom tab bar (see BottomNavigation `tabBarStyle`).
 * Content behind the absolute tab bar must leave at least this much bottom space,
 * plus `useSafeAreaInsets().bottom` for the OS gesture / nav bar.
 */
export const TAB_BAR_BASE_HEIGHT = 70;
